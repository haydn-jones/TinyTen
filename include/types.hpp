#pragma once

#include <cstdint>
#include <vector>

namespace tt::inline v1 {
    // using SizeType = int64_t;
    // using IndexType = std::vector<int64_t>;
    using SizeType = int64_t;
    using IndexType = std::vector<int64_t>;
};  // namespace tt::inline v1